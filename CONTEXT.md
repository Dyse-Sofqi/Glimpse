# Glimpse

Obsidian 插件，基于编辑器选中内容或正则查询动态高亮文本，并提供高亮索引与提词器。

## 语言

**高亮器（Highlighter）**：
一条「搜索查询 → 标记样式」配置。根据查询匹配文档文本并施加视觉标记。
_Avoid_: 样式、规则、高亮样式

**选择高亮（Selection highlight）**：
编辑器选中文本后自动高亮其全部出现位置的临时行为。区别于持久高亮，不落盘。

**持久高亮（Static highlight）**：
用户定义查询与样式的持久高亮器，存储在设置中，跨会话生效。

**标记类型（Mark type）**：
持久高亮器可选组合的装饰方式：`match`（匹配文本）、`line`（匹配所在整行）、`group`（捕获组子内容）、`start`/`end`（匹配起止零宽 widget）。

**捕获组（Capture group）**：
正则查询下用 `(?<name>…)`（命名）或 `(…)`（无名）标记的子表达式，仅高亮子匹配内容而非整段。勾选捕获组时自动跳过整段匹配。

**高亮索引（Highlight index）**：
侧边栏视图，按文档标题层级组织展示 `==...==` 包裹的高亮文本卡片。

**提词器（Teleprompter）**：
独立浮动窗口，在 Obsidian 窗口内置顶显示提取的文本内容。桌面端专用。类似桌面歌词/便签。
_Avoid_: 提词窗口、悬浮窗

**行提取模式（Line-extract mode）**：
提词器内容来源为编辑器光标所在行的文本。上一项/下一项在文档行间移动。

**高亮提取模式（Highlight-extract mode）**：
提词器内容来源为点击高亮索引卡片。上一项/下一项在当前文档的 `==...==` 匹配列表间移动。

**选中提取模式（Selection-extract mode）**：
编辑器选中文本时临时覆盖提词器显示内容；取消选中恢复原模式内容。正交于行/高亮两种模式。

**文档绑定（Doc binding）**：
提词器的检索来源。**跟随（following）** 时随活动文档切换；**锁定（bound）** 时固定到某文档，活动文档切换不影响检索源。
_Avoid_: 文档锁定、源文档

**穿透锁定（Click-through lock）**：
提词器锁定状态。整个窗口鼠标事件穿透到编辑器，仅保留上一项/下一项/穿透锁定按钮可交互；不显示背景。
_Avoid_: 穿透模式

**宽度锁定（Width lock）**：
提词器显示域宽度固定，不再随内容自适应，超出文本换行。解锁后恢复内容自适应宽度。

**上一项/下一项（Prev/Next）**：
提词器导航。行提取模式按行号 ±1（严格，空行显示空白）；高亮提取模式按匹配列表索引 ±1。
_Avoid_: 上一条/下一条
