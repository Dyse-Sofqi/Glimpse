# 更新日志

#### 1.0.9 (2026-09-20)

**新增功能**

- **新增「音乐」模块（复刻 LyricFlux 功能）**：
  - **歌曲播放**：侧边栏默认显示歌单面板（歌曲列表/搜索/筛选/下载入口，当前行高亮跟随切歌），歌词面板经底栏「歌单/歌词」按钮或歌单右上角话筒图标切换；实时歌词当前行放大高亮、非当前行缩小淡化，点击歌词行跳转；逐字高亮（卡拉OK）按字/词变色 + 光晕，支持增强 LRC 的精确 `<mm:ss.xx>` 时间戳（无精确标记按行时长均分）；双语注释 `原文 | 译文` 竖线语法，译文灰色小字且不参与逐字高亮
  - **歌单来源**：设置「音频文件夹」指定 vault 内文件夹或库外 Windows 盘符绝对路径，扫描 MP3/FLAC/M4A/OGG；音频内嵌标题/歌手/专辑/封面自动填充歌单；歌词优先读取音频同目录同名 `.lrc` 侧车文件（Obsidian 内编辑保存即时刷新正在播放的歌词），其次内嵌歌词（MP3 USLT / FLAC Vorbis / M4A ©lyr / OGG comment），文件改动自动重读
  - **播放控制**：底栏两行布局——第一行「歌单/歌词」切换按钮（lucide 图标 + 当前面板文字，置于歌名前）+ 可选中复制的歌名；第二行播放模式、**上一首 / 播放暂停 / 下一首**（三键同组，与播放键共用 `clickable-icon` 样式；试听会话在试听队列内前后移动，本地会话走本地歌单；无播放会话时与定位按钮一同置灰）、可拖动进度条（accent 填充联动播放进度，拖动即时跳转）、时长、倍速弹列表选择（0.5–2x）、水平音量滑条弹窗（为容纳新增按钮并避免窄侧栏裁切右端音量按钮，第二行组间距 7px→5px、底栏左右留白 10px→8px、进度条收缩下限 60px→40px）；底栏图标按钮统一挂原生 `clickable-icon` 类（lucide 图标、悬停背景高亮、默认箭头光标，播放模式激活态走原生 `is-active` 强调色）；「歌单/歌词」切换按钮自底栏上移至两个面板的标题栏右侧（图标 + 当前面板文字，歌词面板标题栏与歌单面板同构并显示行数计数，标题栏去分割线），底栏第一行仅保留可选中歌名；底栏进一步紧凑化（去顶部分割线、收紧内边距与行距）；命令面板快捷命令（播放/暂停、上一首、下一首）
  - **后台播放与进度记忆**：切换应用/最小化后音乐继续播放（移除最初版本的最小化自动暂停，Electron 音频本身不受窗口失焦影响）；退出 Obsidian 时自动保存正在播放的歌曲与进度（播放中每 5 秒节流写盘，暂停/切歌/退出即时保存），下次启动加载上次播放的歌曲、跳转到上次进度但保持暂停不自动播放；歌曲已被删除或不在歌单时静默跳过恢复
  - **歌单标签页定版**：三个标签（本地歌曲/在线歌曲/推荐歌单）改为整行等宽的分段式选择器（图标 + 文字，激活段浮起 + 强调色文字），默认激活「本地歌曲」并直接渲染列表与分类（修复打开面板后无激活标签、内容空白需手动点选的问题，同时覆盖首扫早于面板打开完成导致列表不渲染的场景）；启动恢复上次播放进度后自动静默挂载音乐面板标签页（不展开侧栏、不抢占当前激活标签），覆盖「重载 Obsidian 未保存工作区导致标签页丢失」的场景
  - **标签编辑**：MP3/M4A 可编辑（node-id3 / 自研 MP4 atom 写入器），FLAC/OGG 只读查看；支持改名与删除（vault 内进系统回收站）
  - **在线歌词多平台**：标签编辑弹窗按网易云 / QQ / 酷狗 / 酷我搜索歌词与封面，候选手动选择导入；歌词面板检索不到歌词时自动搜索四平台并给出候选列表，点击候选即保存为音频同目录同名 `.lrc` 侧车文件（不碰音频二进制，FLAC/OGG 等不可内嵌格式同样适用）并立即显示；空态提供「在线搜索歌词」手动重搜按钮
  - **多平台下载（并入侧边栏歌单面板）**：歌单面板改为三标签页（**账号歌单 / 在线歌单 / 本地歌单**），共用顶部搜索框与分类下拉（本地=专辑类型，在线=平台，账号歌单无平台筛选）；在线搜索四平台边输边出（防抖 300ms），**按关键词相关度排序**（不再按平台来源排序），行内试听/下载，下载自动内嵌歌词/封面/标题；网易云可选会员 Cookie 解锁 VIP 高音质（weapi/eapi）；同名文件自动加来源后缀防覆盖；设置页「平台优先级拖动排序」随之移除
  - **账号歌单（网易云登录同步）**：新增「账号歌单」标签——用网易云 Cookie 拉取账号全部歌单（创建 + 收藏，分页拉取），卡片展示封面/曲目数/创建者，点开浏览曲目（行内试听/下载）；歌单头部「同步到本地」按钮批量下载整张歌单到音频文件夹（内嵌歌词/封面/标题，按文件名跳过已存在，带进度与结果汇总，需确认弹窗）；**首次同步完成后音乐侧边栏默认打开「账号歌单」标签**（未同步时默认本地歌单）；未登录网易云时该标签展示登录指引；**修复「测试连接有效但账号歌单报 Cookie 过期」的假阳性**：匿名 Cookie（缺 MUSIC_U）请求账号接口同样返回 code=200，旧判据把未登录误报为「有效（普通账号）」——现以 profile.userId 判定登录态，未登录时明确提示缺 MUSIC_U；网易云 Cookie 获取提示同步改为「F12 → 网络 面板复制完整请求头 Cookie」（新版登录的 MUSIC_U 为 HttpOnly，控制台 document.cookie 读不到）；**修复部分歌曲 VIP 误标**：网易云 fee=8 的歌是非会员可免费播放（标准音质），旧判定 fee>0 一律标 VIP，现仅 fee=1（VIP）/4（数字专辑）标注；歌单钻入视图的标题栏改为**吸附固定**，滚动曲目时 ← 返回/同步/刷新按钮始终可点；**三个按钮（←/同步/刷新）改挂原生 clickable-icon**（样式与悬停特效与底栏播放器一致：默认箭头指针、悬停背景高亮、主题图标尺寸）；歌单卡片名/歌单标题/歌曲标题统一使用界面字体（--font-interface，与文件列表一致）；**账号歌单行不再标「网易云」来源（仅歌单标题栏保留标识）**；**云盘状态接入**：进入账号歌单自动拉取云盘已存歌曲索引（weapi/v1/cloud/get 分页；ID + 文件名双索引——未匹配曲库的上传会得到新云盘 ID，按文件名兜底匹配）——已在云盘的行显示云图标，未在的行显示「上传到网易云云盘」按钮（拉取标准档音频字节 → 缓存优先 → **cloud/upload/check 预检拿云盘曲目 ID** → token/alloc 申请凭证 → NOS PUT → upload/cloud/info/v2 登记，返回 code 200 即完成、无需 publish；MD5 命中服务端已有字节时按 md5 去重秒传）；上传成功行图标即时切换。**修复云盘登记 code 400**：info/v2 的 songid 必须是 cloud/upload/check 预检返回的云盘私有曲目 ID（传 "0" 会被判参数非法），此前缺预检步骤导致所有上传报 400（链路经真实请求逐级实测修正）；**云盘识别补歌名+歌手兜底**：网易云按音频内容判重——同一音频可能以别的文件名或新 ID 存在（此前只按 ID/文件名匹配，导致这类歌仍显示上传按钮、点了秒传返回成功却看不到新条目），现 ID / 文件名 / 歌名+歌手 三路命中即视为已在云盘；**全新音频的上传登记会被服务端拒绝**（uploadStatus 6，账号侧云盘上传策略限制，非插件可绕过；配额实测远未满 12.5GB/60GB，webapi/eapi 两种加密、PUT 主机与参数、码率、调用顺序全试过且用真正全新的歌曲复现）——**据此移除云盘上传入口**（上传实现与相关接口一并删除，避免保留必然失败的按钮）：账号歌单行改为**常驻搜索按钮**（无论是否已在云盘都保留），点击跳转「在线歌单」标签并按歌名自动搜索；**已在云盘的云盘标识移到元数据胶囊行末尾**（云图标 + 「云盘」文字胶囊，与时长/大小/码率等胶囊同排）；**本地歌单行复刻账号歌单行样式**：本地歌曲行改为与在线/账号结果行同构——封面缩略图 + 歌名 + 歌手·专辑 + 元数据胶囊行（格式 + **时长 + 大小** + 已在云盘的「云盘」胶囊）+ 行内按钮区（分组 / 标签编辑或查看 / 搜索 / 播放 / 下载完成态 ✓），不再只是两行文字；**本地歌曲行改为双击播放**（单击不再触发播放，避免滚动/浏览时误触），并新增悬停提示——底色浮起 + 阴影 + 封面浮层淡入（**不描边、鼠标指针保持默认箭头**），行 title 标注「双击播放」；**播放入口移到封面浮层**：浮层即播放/停止按钮（未悬停时不占点击），单击播放并切到歌词面板，当前播放行悬停时浮层显示**暂停**图标、单击即暂停（暂停态点则续播）；**行尾单独的播放按钮已移除**（功能并入封面浮层），**行尾的「已下载 ✓」标识也一并移除**（本地歌单里的歌本就是下载好的，标识多余）；**账号歌单与在线结果行同样改用封面浮层播放**：悬停效果与本地行完全一致（底色浮起 + 阴影 + 浮层淡入，不描边、默认指针），行尾播放按钮移除——本地已下载的行点浮层播放本地文件（播放中浮层显示暂停图标，点即暂停/续播，播放态经 `data-local-path` 增量跟随），未下载的行点浮层试听（试听中浮层**常驻显示**强调色底 + 停止图标，再点停止，兼作「哪首在试听」的标识）；账号行行尾保留「已下载 ✓」与下载按钮（用于区分本地已有状态）；**时长与大小在歌单富化时探测**（大小取文件 stat；mp3 时长按文件头估算——Xing/Info 帧数精确、否则按 CBR 推算，不读全文件），富化完成自动刷新列表补齐胶囊；当前播放行保留选中底色 + 左侧强调色竖条；原本地行的独立样式（`gm-songs-item*` 系列与悬浮按钮）已随重写移除；**账号歌单行新增「已在本地歌单」状态**：与本地歌单比对（文件名 + 歌名/歌手双路匹配，本地歌单变化时重建索引），本地已有的行**下载按钮直接呈完成态绿 ✓**（与下载成功后的按钮状态一致），与云盘标识**互不竞争**——云盘图标/上传按钮独立显示（不再合并为单一状态）；**云盘图标改为与行内按钮同尺寸同图标大小的图标位**（原小胶囊 12px 且高度被挤压），沿用底栏按钮的观感；**本地已有的行播放按钮走本地播放链路**（直接播放本地文件、带侧车/内嵌歌词并切到歌词面板），不再走试听缓存流；下载/同步完成后账号歌单行状态自动刷新（保持滚动位置）
  - **标签重组（推荐歌单并入在线歌单）**：在线歌单标签搜索框**无内容时展示推荐歌单首屏**（含四来源胶囊切换），**有关键词时先列匹配的在线歌曲、后列「匹配的在线歌单」小节**（按名称过滤已缓存的四源推荐歌单，首次搜索自动后台补齐缓存）；推荐歌单的卡片/歌内曲目/刷新视图全部复用
  - **修复标签内容残留与歌单返回失效**：标签内容区为三标签共用容器，账号歌单视图各分支此前未先清空——切到账号歌单（或歌单内点 ← 返回）时本地歌曲列表/歌单曲目残留、新内容追加在下方，表现为「账号歌单残留本地列表」「返回按钮无效」；现标签分发与各视图入口统一先清空再渲染（在线歌单无关键词分支同步修复）
  - **下载前试听**：结果行「试听」拉取标准档音频直接播放（不写入库），同歌再点命中会话内缓存秒播；设置页可一键释放缓存
  - **推荐歌单**：下载弹窗首屏四源推荐歌单胶囊切换，点歌单逐首下载
  - 实现：新增 `src/music/` 模块（约 30 个文件），播放器以 vanilla DOM 重写（无 Svelte 依赖）；新增依赖 node-id3、jsmediatags（打包进产物，main.js +700 KB）；设置页新增「音乐」页签；音乐相关样式以 `gm-` 前缀与 LyricFlux 共存不冲突；各平台 Cookie 行新增「打开登录页」按钮，直达网易云/QQ 登录页与酷狗/酷我官网（登录入口在右上角），登录后 F12 复制 Cookie 即可粘贴；提示中的 document.cookie 做成可点击代码芯片，点击即复制（图标短暂变 ✓ 反馈）；QQ 行提示单独说明其凭证 Cookie 为 HttpOnly（document.cookie 读不到），需登录并播放歌曲后从「网络」面板复制 Cookie 请求头；**实测修复：QQ 免费歌曲无需 Cookie 即可下载/试听（免登录可拿直链），下载/试听/测试连接不再硬性要求 Cookie**——旧 GetUserInfo 校验端点对新版登录体系恒返 500003（有效 Cookie 也误报），已改为用免费探针歌曲的 vkey 请求探测真实下载通道，VIP 歌曲失败时如实提示需绿钻；设置页「下载」分组顶部新增免登录说明（免费歌曲四平台免登录可下、Cookie 仅 VIP 场景需要），酷狗 / 酷我行提示同步标注「下载不使用 Cookie」；**修复歌单列表滚到底时最后一项被底栏遮挡**：底栏由悬浮（absolute）改为文档流内布局（flex 末尾元素），面板滚动区不再需要为其预留 padding，歌词面板同步受益；**根因进一步修复：音乐面板容器移除 Obsidian 默认 `view-content` 类**——其默认样式带 `padding-bottom: max(safe-area, 32px)`（此前用 `:has()` 覆盖，旧渲染引擎不支持导致底栏下方大量留白），容器高度改为 `calc(100% - var(--header-height))` 自算；最终方案：宿主 leaf-content 挂 `gm-view-host` 类接管为 flex 列布局（padding 清零），容器 `flex: 1` 精确填满 view-header 之外的全部剩余空间，彻底消除底栏下方留白（不再依赖主题的 --header-height 变量）；新增「底部状态栏适配」设置开关：**开关直接切换 gm-panel-container 容器上的 view-content 类**（修复：曾误切到内部歌单面板导致留白位置无效、gm-statusbar 仍被应用状态栏遮挡）——开启后保留 Obsidian 默认样式（面板底部为悬浮的应用状态栏预留安全区留白，适配状态栏固定悬浮的主题），关闭（默认）移除该类贴边铺满，切换即时生效；音频文件夹设置新增「浏览」按钮，打开系统资源管理器选择文件夹（参考 ziping 的 `app.electronRemote.dialog` 双通道方案；选 vault 内目录存相对路径、vault 外存 Windows 绝对路径）；「逐字高亮（卡拉OK）」与「底部状态栏适配」默认值均为**关闭**
- **提词器模式切换下拉化 + 新增「歌词提取」模式**：模式切换由「逐行提取/高亮提取」双态文本按钮改为下拉列表（保留原生下拉箭头，收起值与弹出选项均居中，指针默认箭头，窗口拖拽/滚轮逻辑对下拉豁免使选项可弹出，穿透锁定时隐藏）；新增第三种模式「歌词提取」——展示音乐模块当前正在播放的歌词行，随播放进度自动滚动换行（按歌曲+行号去重，不因高频状态推送重绘）；上一项/下一项/滚轮改为跳转到相邻歌词行的时间戳（暂停时同样生效）；无播放会话显示「未在播放歌曲」占位提示、前奏未唱到第一行显示「♪ 前奏 ♪」；实现依赖初始化顺序调整：MusicManager 先于 TeleprompterManager 创建（提词器窗口构造时订阅音乐状态）
- **修复歌词高亮慢半拍**：换行判定原先仅依赖 audio 的 timeupdate 事件（约 250ms 粒度），行切换最坏滞后一个事件周期，体感即「歌词慢一拍」；播放期间新增 requestAnimationFrame 帧循环直接读 currentTime，行切换在时间戳越界后的下一帧（~16ms）内推送（侧边栏歌词高亮/滚动与提词器「歌词提取」模式同步受益；窗口隐藏时 rAF 暂停，由 timeupdate 管线兜底）
- **试听临时歌词**：试听歌曲（无本地文件）的歌词面板支持在线搜索并**临时挂载**歌词——空态与「换歌词」按钮均可用，候选点击后歌词挂内存仅本次试听有效（随播放进度高亮/滚动/卡拉OK，不写任何文件），切歌或试听结束自动失效；自动歌词搜索对试听同样生效
- **自动匹配并应用在线歌词**：本地歌未检索到歌词时，歌词面板打开会自动联网搜索四平台，并**按相关度顺序自动应用第一份有歌词的候选**（保存为侧车 .lrc 并立即显示；试听则临时挂载）；全部候选都无歌词时退回手动候选列表；手动「在线搜索歌词 / 换歌词」按钮仍展示候选列表供自行挑选
- **网易云客户端缓存目录（试听/下载离线秒开）**：设置 → 音乐新增「网易云客户端缓存目录」选择器——指向网易云音乐客户端的缓存目录（客户端 设置 → 下载与缓存 里可查看，默认 `C:\Users\<用户名>\AppData\Local\Netease\CloudMusic\Cache\Cache`）后，「试听」与网易云歌曲下载（**含账号歌单同步**）优先读取客户端已缓存的音频（文件名 `<歌曲ID>-<码率>.mp3/.flac`，同 ID 取最大文件），命中即离线秒开、不再联网；音质取决于客户端当时缓存的档位，未命中照常走在线链路
- **歌词偏移微调（每首歌独立）**：歌词面板标题栏新增 ±2000ms 偏移滑块（步进 100ms，负值=歌词提前），拖动即时重定位换行与卡拉OK逐字判定（暂停时同样生效，点歌词行跳转仍按原始时间戳）；偏移按音频路径持久化在 data.json 的 music.lyricOffsets（不产生额外文件，内嵌歌词同样可用），未微调的歌默认 **-150ms**——微小的提前量抵消音频输出等残余延迟，宁可提前不落后
- **修复循环播放播完即停 + 播放模式收敛为三种**：单曲循环在歌曲自然结束时音频已暂停，原先只 seek(0) 不续播，播完一遍就停（歌单仅一首的顺序/乱序兜底同理），已补显式播放；播放模式去掉「关闭」档——它与顺序播放共用 repeat 循环图标、仅靠激活色区分，极易误选（关闭模式每首播完即停，正是「循环播放播一遍就停」观感的来源）；现收敛为单曲循环（repeat-1）/ 顺序播放（repeat）/ 乱序播放（shuffle）三种轮流切换，**仅图标变化、无激活强调色**，旧数据 playMode 为 off 时启动归一化为顺序播放
- **本地歌单自定义分组**：歌曲行新增「分组」悬浮按钮（与标签按钮并排），菜单内勾选所属组、新建分组、移出分组；列表按分组**手风琴**展示——组头为吸附式背景条（滚动时钉在列表顶部），组名复用主题粗体样式（--bold-weight/--bold-color，跟随主题设置；字体用文件列表文件夹的界面字体 --font-interface），左侧 grip-vertical 把手（图标与底栏一致的主题尺寸）**按住拖动可调整分组顺序**（组序持久化，搜索/筛选视图不显示把手），组名 + 数量，**默认全部折叠**，点击展开当前组并折叠其他组、再点当前组折叠，组内行相对缩进，当前播放行左侧强调色竖条，列表行距收紧；悬停 ⋯ 按钮或右键组头可重命名/删除分组（删除只解散分组、歌曲文件不动），归组/移出操作后自动展开目标组，搜索/筛选中自动展开全部有结果的组（否则折叠组会让结果看似为空）；未指派的歌进「未分组」区块；分组数据存 data.json 的 music.songGroups（组名有序列表 + 路径归组映射，**不产生额外文件**），重命名连带更新成员归属，音频文件改名/移动后归组关系丢失（与歌词偏移同限制）；未建任何分组时保持原扁平列表，搜索/筛选中自动隐藏空组；本地歌单滚动条隐藏（滚轮/拖拽滚动不受影响）
- **顺序播放按分组范围**：正在播放的歌曲属于某个自定义分组时，顺序播放的「下一首/上一首」（含播完自动续播、底栏上一首/下一首按钮与命令面板快捷键）只在该分组内循环——分组末首播完回到分组首首，分组首首的上一首是分组末首；分组内只有一首时等效单曲循环。未分组的歌曲、以及没有建任何分组时，仍按整张歌单顺序（行为不变）；单曲循环与乱序不受影响。实现为 `sequentialScope()`：范围 = 当前歌单顺序过滤出该组的歌，因此拖拽排过的顺序在分组内同样生效
- **本地歌单拖拽排序**：按住任意歌曲行上下拖动即可调整顺序（5px 阈值防误触，拖到列表上下边缘自动滚动；分组视图内只在同组内换位，不跨组——跨组等于改分组归属，属另一个操作）；顺序存 data.json 的 `music.songOrder`（不产生额外文件），**同时决定顺序播放模式的切歌顺序**；重扫歌单与标签富化后的重排都以自定义顺序优先，不会把排好的顺序冲掉，写回时自动清理已删除歌曲的残留路径；搜索/类型筛选视图下禁用拖拽（只渲染命中的行，按 DOM 顺序写回会丢掉未渲染歌曲的相对顺序）；拖动中的行有抬起态（阴影 + 抓取指针，平时保持默认箭头）并隐藏封面播放浮层
- **试听经底栏主播放器播放（修复独立通道不可控与并发）**：试听此前用独立 Audio 元素播放——与底栏正在播放的本地歌曲叠加出声，且播放/暂停/进度/音量全部不可控；现试听**直接接管底栏主播放器**（播放器新增 blob 音源类型，Blob URL 生命周期移交播放器、销毁时释放），底栏播放/暂停/进度/音量与歌词面板全部适用，本地歌曲开始播放时试听播放器照常被销毁且行按钮图标自动复位；试听不写入 lastPlayed/歌词偏移，也不触发歌词自动在线搜索（preview: 合成路径不落盘）；**试听播完按播放模式在队列内续播**——发起试听时所在的曲目列表（歌单钻入列表/在线搜索结果）即为试听队列：单曲循环重播本曲（试听缓存秒开）、顺序播放取下一首（循环）、乱序随机换一首、关闭则自然停止；**加载失败（VIP 受限/版权）自动按播放模式跳下一首**（每首失败弹「无法加载，已自动跳过」提示，整队列都失败才停止并汇总）——**首次点击试听失败同样跳过**（此前跳过只在续播链路生效，首点失败只提示不跳）；**字节能下载但音频解码失败（VIP 占位字节）同样自动跳过**（audio error 事件接入队列跳过逻辑，连续失败计数共享，成功出声归零），续播自动重新拉取字节（命中试听缓存/客户端缓存即秒开），行按钮的 stop 图标随续播自动转移到新行；**修复「需要点两次才播放」**：拉取中的重复点击此前会取消重发（第一次永远被第二次覆盖）、扫描清理会静默停掉试听播放器、播放启动失败被静默吞掉——现拉取中重复点击直接忽略、扫描不再打断试听（preview 路径不参与歌单清理）、播放启动失败弹提示并可用底栏播放键恢复；**播放启动增加生效验证与自动重试**（play() 决议后短暂等待确认真的开始播放，150ms/400ms 两段复查未出声自动重试拉起，仍失败才提示）——修复「拉取到 100% 后无事发生，需再点一次（命中缓存秒开）才播放」：拉取完成后的字节只要播放位未被其他内容实际占用（没有正在播别的歌）就一定播出，不再因中途状态推送吞掉整次试听；**修复启动试听被自身中止而无法播放**：stopAllPlayback 的空状态推送会误触接管守卫清掉 previewKey，拉取完成后被自身完成检查中止——现先等停止完成再标记试听行，接管守卫对空状态免疫
- **换歌词按钮**：歌词面板标题栏（切换按钮前）新增 refresh-cw 按钮——对已有歌词的歌曲重新在线搜索四平台歌词，候选列表展示在歌词列表上方（独立容器，不被播放进度触发的歌词重绘清掉），点击候选即覆盖音频同目录的侧车 .lrc 并立即生效（FLAC/OGG 等内嵌歌词同样可用，侧车优先级最高）；再次点击按钮或切换歌曲自动收起候选，无播放会话时置灰
- **底栏「定位当前歌曲」按钮**：歌名前新增 locate-fixed 按钮，点击切换到本地歌单、自动展开当前歌曲所在分组并**居中滚动**到当前行（手风琴折叠状态下也能直达）；无播放会话时置灰不可点


**错误修复**

- **修复分组拖拽把手无法调整顺序**：原实现依赖 setPointerCapture 把后续 pointermove 重定向到把手，但把手节点在拖动中会被移动 DOM 位置，部分环境下捕获失效导致 move 事件丢失、拖拽无反应；改为 **move/up 监听挂 window**（不依赖捕获，任何情况下不丢事件），并新增拖至列表上下边缘的自动滚动、拖拽后抑制浏览器补发 click 误触手风琴
- **修复音量按钮偶发弹不出滑块（播放/暂停、模式按钮同理的偶发点击失效）**：底栏与悬浮按钮的图标随每次状态推送（播放中 ~4 次/秒）无条件重建 svg——按压按钮的瞬间（mousedown → mouseup 之间）恰逢推送，正被按住的 svg 被移出 DOM，浏览器不为该次按压派发 click，点击静默丢失；现图标**仅在变化时重建**；顺带修复音量弹窗「点击外部关闭」监听的卸载事件名不匹配（注册 mousedown / 移除 pointerdown，导致监听永远卸不掉、越积越多）：统一为 pointerdown，并把音量按钮自身排除在「外部」之外（此前依赖按钮拦截 mousedown 冒泡实现切换语义）
- **修复本地歌单分组列表悬停变透明**：分组组头与歌曲行的悬停底色原用 `--background-modifier-hover`，而该主题变量是半透明色（`color-mix(…, transparent)`）——悬停时行与吸顶组头会透出面板底色，观感「发空」；改用主题色阶里不透明的 `--background-secondary-alt`（浅色 `#fcfcfc` / 深色 `#333333`），仍是「浮起一档」的悬停反馈，其余悬停效果（行阴影、封面播放浮层淡入、组头 ⋯ 按钮显形）不变
- **修复歌词自动滚动把手动上滚立刻复位**：此前每拍 timeupdate 都会把脱离可视区的当前行拽回居中，且换行重建（`empty()`）会把视口弹到列表顶部——想往上翻看歌词时刚滚上去就被拉回；现在手动滚动（滚轮/触摸/拖滚动条）即暂停自动跟随，往上翻看时视口钉在原地、换行按锚点恢复不跳动，滚回当前行完整可见后自动恢复跟随（换歌或重新进入歌词面板同样恢复）
- **修复提词器重启后被抬升一段距离**：位置钳制原按「窗口高度」计算（`y = min(y, vh - h)`），而窗口高度由当前行文决定，于是任何与位置无关的高度变化都会把整窗顶上去——重启时构造期还没有活动文档（内容空、窗口矮），保存的 y 原样保留；文档打开后真内容渲染变高、宽度自适应再次摆位时就被钳上去（且新位置被写盘，每次重启继续往上爬）。现在纵向只保证顶部可见（不再按高度钳制），装不下的长行文改由内容区高度上限（`min(70vh, 窗口下方可用空间)`）消化并在窗口内滚动；改字号同样不再移动窗口，拖拽仍保持「整窗不出视口」


#### 1.0.8 (2026-09-15)

**新增功能**

- **自定义 CSS 输入框升级为 CodeMirror 6**：获得语法高亮、括号与引号自动闭合、撤销历史，以及**按输入自动补全 CSS**（属性名、属性值关键字、选择器标签名）。输入即弹出补全面板，也可 Ctrl/Cmd-Space 手动唤起；选中属性会连带插入冒号与空格。补全词表取自 `@codemirror/lang-css`，随 CSS 规范更新，无需人工维护（产物 +29 KB）
- **持久高亮新增「描述」字段**：备注该表达式匹配什么内容，仅用于设置页展示、不参与匹配。设置页里排在名称下一行，卡片里排在表达式下一行；随导入导出与剪贴板往返
- **设置页表单重新分行**：「搜索词/表达式」输入框与其正则开关独占一行（此前与名称、色板挤在同一行）；「清空当前编辑」「从剪贴板导入」「保存」三个按钮移到名称与色板之后
- **新增「清空当前编辑」按钮**（lucide `refresh-ccw`）：一键把表单恢复初始状态（名称、描述、色板、搜索词、正则开关、标记开关、自定义 CSS），放弃编辑已有样式时不必逐项手动清除
- **提词器字重扩展到 100–900**：原 300–700，补 100 极细、200 特细、800 特粗、900 极粗
- **背景色默认置空**：新建高亮器不再预设默认背景色；未选色时 `color` 存空串，静态高亮器自动跳过背景色（高亮类照常生效，可交由自定义 CSS 接管）

**错误修复**

- **修复「点色板圈圈拖不动，点圈圈之外才能拖」**：圈圈（`.pcr-picker`）与交互面（`.pcr-palette` / `.pcr-slider`）在 Pickr 模板里是**兄弟节点**而非父子，原拖拽桥接只把 pointer 监听挂在交互面上，按住圈圈时 pointerdown 不冒泡到该监听器，`dragging` 恒为 false。现两者都挂监听、`touch-action: none` 一并设置，`setPointerCapture` 改用 `e.currentTarget`（实际被按下的元素），拖出弹层范围也能连续拖动
- **修复「编辑未设背景色的高亮器，什么都没改，保存却被写入默认色」**：Pickr 的 `getSelectedColor()` 返回内部 `_lastColor`，而 `_lastColor` 只在 `applyColor()` 里更新——`setColor(hex, true)` 走静默分支不会调用它；`setColor("")` 更是在颜色解析失败后直接 `return false`，状态原封不动。色板状态改为自行维护（含「打开弹层前快照」，取消时正确回滚），编辑 / 剪贴板导入 / 重置三处对空颜色显式清空
- **修复设置页色板实例泄漏**：`display()` 重入时不释放上一轮的 Pickr 实例；现改为统一的销毁清单，重建与关闭时都会先释放
- **修复编辑按钮中 `setColor()` 被重复调用两次**

**重构与维护（对外行为不变）**

- **设置界面取色代码独立重写**：三处重复的 Pickr 集成（持久高亮背景色、提词器字体颜色、渐变停靠点）收敛为 `src/settings/color-picker.ts` 的 `createColorPicker()` 工厂，回调式接口、`destroy` 幂等；调用点由约 125 行重复代码降为 3 处薄调用
- **移除上游派生代码，协议维持 MIT**：删除 `setAttributes()`（逐字复制且已无调用方）与死 CSS（`.highlighter-item-draggable`、`.highlighter-sortable-grab`）；类名 `highlightr-color-picker`、`highlighter-settings-*`、`highlighter-sortable-*`、`highlighter-setting-icon-drag` 统一为 `glimpse-*`；内置 pickr 样式段补上来源与 MIT 版权声明
- **设置页骨架改为表驱动**：`MAIN_TABS` 声明四个页签，`registerDisposable()` 统一管理资源释放
- **消除审核灰区**：预览样式的 `setAttribute("style", …)` 改用 `setCssProps` + 声明体解析（`obsidianmd/no-static-styles-assignment`）

#### 1.0.7 (2026-09-15)

- **选择高亮装饰改为「有结果才出现」**（行为变更）：匹配文本双下划线与滚动条标记仅在全文检索到**选区之外的至少一个出现位置**时出现；存在其他匹配时表现与之前一致（选区仍标记 `cm-current-string`，选区所在行仍保留滚动条标记）
- **修复选中唯一文本时仍出现装饰特效**：`getDeco()` 以 `from <= selFrom && to >= selTo` 判定选区自身，而选区必然命中查询，使「匹配数 ≥ 1」恒成立，装饰与滚动条标记无条件出现；现区分「选区自身」与「其他匹配」，无其他匹配时返回 `Decoration.none` 并清空滚动条标记，`maxMatches` 截断不受影响（判定位于截断之前）

#### 1.0.6 (2026-09-14)

- **通过 Obsidian 插件审核（`obsidianmd/no-static-styles-assignment`）**：1.0.5 新增的「文字渐变」设置中，渐变角度项随渐变类型切换显隐时直接赋值 `settingEl.style.display`（审核规则禁止静态样式赋值）；改为封装 `updateAngleVisibility()` 并用 Obsidian 元素的 `hide()`/`show()` API 切换，行为不变

#### 1.0.5 (2026-09-14)

- **提词器外轮廓阴影常驻**：面板外轮廓阴影（`--shadow-s`）由悬停/拖拽/缩放时附加改为常驻显示，拖拽与缩放过程中外观保持一致；隐藏背景模式下仍随背景一并隐藏（穿透锁定状态同样常驻）
- **新增隐藏背景字幕投影**：激活「隐藏背景」后为正文文字添加柔和投影（字幕感），文字浮在文档上更易读；恢复背景后投影自动消失，空内容占位文本不带投影
- **新增设置「文字阴影」分组**（可折叠标题）：投影开关、水平偏移（-20~20px）、垂直偏移（-20~20px）、模糊半径（0~40px）、不透明度（0~100%，固定黑色）五个设置项，均带「重置为初始值」按钮；默认值按字幕效果校准（右偏 2px、下偏 3px、模糊 6px、不透明度 60%），参数经 CSS 变量注入、改动即时应用到所有已打开窗口
- **新增设置「文字渐变」分组**（可折叠标题）：渐变开关（开启后覆盖「字体颜色」）、线性/径向渐变类型、整体/逐字渐变范围、渐变角度（0-360°，径向时自动隐藏）、颜色停靠点编辑器（增删停靠点、位置滑条、色板，按位置排序渲染，至少保留两个）与实时预览（与提词器渲染同参数，所见即所得）；逐字渐变将文本节点字符包进 span 每字独立裁切渐变（`Intl.Segmenter` 按字素切分，emoji/组合字符不拆碎，空白不包裹保持换行），默认红系渐变（#FF4D4F → #B00020，90° 从左到右）
- **修复投影浮在文字上面**：`text-shadow` 画在文字后但会透过半透明字形（字体不透明度 <100% 或渐变透明填充）渗出，看似叠在文字上；改用 `filter: drop-shadow()` 从渲染后的文字像素轮廓向外投射，阴影严格位于文字之下
- **修复设置页折叠分组样式**：折叠标题改用 Obsidian 原生 heading 样式（名称加粗放大、整行可点折叠/展开）；修复折叠体第一个/最后一个设置项顶部与底部被截断（误清卡片式设置项的内边距所致）

#### 1.0.4 (2026-08-15)

- **修复持久高亮列表拖拽图标垂直居中失效**：Obsidian 新版设置项（`.setting-item`）默认 `align-items: flex-start`，行内子元素顶部对齐，`grip-vertical` 拖拽图标悬在行顶（颜色预览此前已用 `align-self: center` 补偿，拖拽图标漏了）；为 `.highlighter-setting-icon-drag` 与 `.setting-item-control` 补 `align-self: center`，图标/预览/操作按钮恢复与行内容垂直居中

#### 1.0.3 (2026-08-09)

- **修复高亮索引首次打开新文档不刷新**（两阶段根因）：① 重渲染仅监听 `active-leaf-change`，新文档在已激活叶子内打开（新建 / 资源管理器点击当前标签页）不触发该事件，补监听 `file-open`；② `collectFromView` 只读 CM 编辑器 `state.doc`，`file-open` 触发时内容尚未加载，首扫误报 0 匹配，CM 内容为空时回退 `vault.cachedRead` 读盘。首次打开即检索，无需切走再切回
- **高亮索引设置项改名并默认关闭**：设置项「默认打开高亮索引」→「启动时默认打开高亮索引」，默认值改为关闭（已存 data.json 不受影响）
- **README 双语整合**：英文版合并进 README.md 末尾（顶部双语提示「拉到下面查看英文版」），删除独立 README.en.md；以中文介绍为蓝本同步修正英文内容（删除英文独有示例、对齐漂移措辞、同步设置项改名）

#### 1.0.2 (2026-08-09)

- **通过 Obsidian 插件审核（二轮）**：`obsidianmd/no-static-styles-assignment` 复抓 8 处 `style.setProperty` 两参调用（minimap / export / pickr-drag / persistent-ui / teleprompter），全部改 `setCssProps`（评审放行 API）；新增 `dom-augment.d.ts` 补齐 0.14.8 缺失的类型声明。规则边界实测：自定义属性（`--xxx`）与带 `important` 三参 `setProperty` 放行

#### 1.0.1 (2026-08-09)

- **修复赞助图无法显示**：README / manifest 中赞助图片链接由 `github.com/{repo}/zanshang.jpg` 改为 `raw.githubusercontent.com` 直链（原路径返回 404 文本，`<img>` 破图）
- **README 亮点优化**：文档开头新增功能关键词队列（动态高亮、正则查询、捕获组、自定义 CSS、高亮索引、提词器、光标联动、滚动同步、缩略图），中英同步
- **通过 Obsidian 插件审核合规**：
  - 静态样式赋值 `.style.X = "字面量"` 全部改用 `style.setProperty` / `hide()`/`show()`（`obsidianmd/no-static-styles-assignment`）
  - 移除 `onunload` 中 `detachLeavesOfType`：卸载时分离叶子会复位到默认位置，用户挪过也丢
  - 动态用户自定义 CSS 注入由 `<style>` 元素改 `CSSStyleSheet` + `document.adoptedStyleSheets`（审核禁止创建样式元素；功能不变，Chromium 全支持）

#### 0.9.10 (2026-08-09)

- **重构高亮索引标签页布局**：删除「高亮索引」「追根溯源」按钮及按钮栏，高亮文本索引直接归属高亮索引标签页
- **新增光标联动**：编辑器光标落入高亮语句所在行时，索引中对应卡片自动选中并滚动至屏幕中央（150ms 轮询检测光标行）
- **新增锚定文档回退**：当前页面检索不到高亮时，自动从提词器锚定（绑定）的文档中检索展示；锚定文档未打开为视图时读盘检索
- **新增键盘导航**：聚焦高亮索引标签页时 ↑/↓ 切换上/下一项高亮卡片（从已选中卡片起算，边界钳制），不滚动面板；同步编辑器光标与提词器
- **新增滚动同步**：提词器工具栏「穿透锁定」右侧新增「滚动同步」按钮（lucide `link`）；开启后上一项/下一项切换同步触发——逐行提取模式光标跳转对应上一/下一行，高亮提取模式选中索引中对应上一/下一项卡片；穿透锁定时随非交互按钮一并隐藏
- **提词器双击联动索引卡片**：高亮提取模式双击提词器文本区，同步选中索引中对应卡片并居中（跨渲染源时索引自动切到该文档）
- **修复插件加载崩溃导致全局 ↑/↓ 失效**：`teleprompterManager` 初始化晚于高亮索引视图 onOpen 的 layout-ready 回调，`anchoredDocPath` 读 undefined 抛错，索引视图整体损坏、键盘导航全部失效；初始化提前至视图注册之前并加空检查防御
- **修复重启后高亮模式提词器无内容**：重启恢复高亮模式窗口时主动加载匹配并定位，滚动（上一项/下一项）按需重扫获取，不再只有点击索引卡片才能刷新
- **修复点击卡片选中态立即消失**：叶子变更重渲染判定由视图实例改为按渲染源文档路径（`renderedPath`），内容未变（含提词器双击回焦编辑器、聚焦本标签页）跳过重渲染，选中态保留
- **修复滚动同步选中被光标轮询取消**：光标联动轮询改用独立 `lastPollCursorLine` 跟踪光标移动，光标静止时不覆盖键盘/滚动同步的主动选中
- **卡片复制改为右键**：删除索引卡片右上角复制按钮，右键卡片复制文本（与提词器右键复制共用 `copyText` 实现）
- **移除悬停键盘导航**：提词器悬停 ↑/↓ 切换与编辑器原生导航冲突（capture 劫持判定任一失效即全局抢键），移除该功能，上一项/下一项由滚轮与工具栏按钮承担

#### 0.9.9 (2026-08-08)

- **修复高亮索引更新后点击提词器显示第一项**：`ensureMatches` 按文档路径缓存匹配，索引重扫后同文档内容已变更，点击卡片仍命中旧缓存、按文本找不到新项而回退到第一项；改为点击时强制重扫（清空缓存），并监听匹配源文档 `modify` 失效缓存——编辑绑定文档后重扫并即时刷新当前匹配（文本未变跳过渲染，避免逐键重绘闪烁）
- **修复滚动浏览后双击跳转选中失效**：双击选中在编辑器留下选区，行模式轮询将其判为「选中提取」置 `selectionOverride`，滚轮浏览下一项后该标志陈旧，再次双击被早退分支短路、只聚焦不跳转；手动浏览（上一项/下一项）时清除该标志，双击恢复跳转选中当前显示项

#### 0.9.8 (2026-08-07)

- **新增提词器正文字体设置**：设置界面「提词器」页签新增「字体」项，点击「选择字体」打开字体选择模态窗——优先用 `queryLocalFonts()` 枚举本机字体，不可用时降级为 canvas 测宽探测内置候选表；可勾选多个字体并按优先级排序（拖拽调整顺序），列表靠前且本机存在的字体优先生效，不存在的自动顺延到下一项（CSS `font-family` 回退语义）；支持搜索、字体名以自身字体渲染预览、手输自定义字体名；留空则跟随主题默认字体
- **新增字重设置**：正文字重下拉（跟随主题 / 300–700），实时应用；字重影响字宽，变更后自动重排窗口宽度
- **新增字体颜色设置**：正文颜色色板（Pickr），点击即存即应用，透明度不参与（由「字体透明度」独立控制）；「清除」恢复跟随主题；弹层从色块按钮左侧弹出；新增「重置为初始值」按钮
- **修复选色器无法拖拽**：Obsidian 拦截 document 级 `mousemove`，而 pickr 内部完全靠它驱动拖拽，导致色板/色相圈圈只能点击、无法拖动；改为监听 pointer 事件直接驱动 pickr 内部拖拽逻辑，绕过拦截（提词器与持久高亮两处选色器一并修复）
- **双击跳转并选中文本**：双击提词器文本显示域，除光标跳到捕获文本所在行外，高亮模式下选中对应匹配文本段、行模式下选中整行、选中提取时保留编辑器现有选择（即所显示文本）；选中范围滚动至视口中央
- **修复自定义字体下宽度测量失真**：auto-fit 宽度探针显式携带当前实际字体，不再回退到主题默认字体测量，换用自定义字体后宽度自适应不再偏差
- **对抗主题字体覆盖**：字体/字重/颜色以内联 `!important` 应用，压过 Blue Topaz 等主题对 `.markdown-preview-view` 容器的高特异性字体规则

#### 0.9.7 (2026-08-07)

- **修复关闭后重开的状态跨重启丢失**：关闭提词器后重新打开（状态从暂存恢复）时，持久化在窗口入列之前执行，把空窗口列表写盘，导致 Obsidian 重启后恢复的窗口消失、再次手动打开回到默认状态；调整持久化时机，重开恢复的窗口状态正确落盘，重启后仍可恢复

#### 0.9.6 (2026-08-07)

- **穿透锁定时隐藏跟踪光标按钮**：工具栏「跟踪光标」按钮不再属于穿透锁定下的交互按钮，激活穿透后一并隐藏
- **穿透锁定时工具栏悬停才显示**：修复穿透锁定激活后鼠标移出窗口按钮栏仍显示的问题——锁定状态下工具栏同样仅在鼠标悬停时显示，移出即淡出
- **关闭后重开恢复状态**：关闭提词器后重新打开，窗口位置、尺寸、模式、绑定文档、跟踪光标等状态完整恢复
- **关闭状态跨重启保留**：已关闭窗口的状态持久化到 data.json，Obsidian 重启后重开仍可恢复；已关闭的窗口不会在重启后自动弹出
- **文本垂直居中**：文本显示域内内容垂直居中；内容超出最大高度时回退常规滚动，顶部不截断
- **新增「选择检索的字符串上限」设置**：选择高亮设置新增滑杆（2-60，默认 30），超过该长度的选中文本不再进行全文匹配，避免超长选择拖慢编辑；滑杆旁带「恢复默认」重置按钮（lucide `rotate-ccw`）

#### 0.9.5 (2026-08-06)

- **新增「跟踪光标」开关**：工具栏新增「跟踪光标」按钮（lucide `text-cursor`），将行模式的光标跟随独立成可开关功能（默认关闭）；开启后光标位置变动时自动提取所在行（仅行模式生效），关闭时行模式静态、手动浏览不受干扰
- **双击跳转光标行**：双击提词器文本显示域，编辑器光标跳到捕获文本所在行并聚焦，目标行滚动至视口中央；高亮模式下跳到当前匹配项所在行
- **右键复制纯文本**：右键单击文本显示域复制捕获文本的渲染后纯文本（无 Markdown 语法），成功时弹出「已复制」通知
- **文本显示域最小高度**：内容区最小高度 45px，防止内容过矮时面板塌缩
- **修复宽度测量失真**：宽度自适应测量探针显式携带当前实际字号，不再被 `.glimpse-tp-content` 类固定 50px 覆盖，测量跟随真实字体档位
- **修复宽度自适应导致窗口累计右移**：行宽差为奇数像素时重排偏移为 ±0.5px，`Math.round` 遇 .5 恒上取造成正负方向不对称，逐次渲染累计右移直至顶到右边界被钳回；改为 `Math.trunc` 向零截断，偏移对称有界

#### 0.9.4 (2026-08-02)

- **导入/导出/自定义 CSS 编辑器改为样式化纯文本框**：CodeMirror 编辑器在多插件环境存在光标、输入与主题兼容问题，改为纯文本框（monospace、`pre` 不折行、边框、可调高度）；文本框高度随内容自适应（带下限/上限，超出滚动），规避环境问题
- **一键导出界面**：按钮移至文本框上方（导出到文件 / 导出到粘贴板），JSON 展示框自适应高度
- **一键导入界面重构**：「从文件导入」改为按钮，粘贴框自适应高度，按钮置顶，与导出界面共用同一套样式；移除 `modal-style-settings` 类，避免 Style Settings 插件的固定 70vh 撑高弹窗
- **提词器主题切换即时适配**：切换浅色/深色主题时，窗口背景色立即更新（MutationObserver 监听 body 主题类 + `css-change` 事件兜底）
- **分组拖动排序**：持久高亮设置中分组标签可拖动调整顺序，默认分组固定首位且不可拖动
- **删除分组按钮补提示**：删除分组按钮补上「删除分组」tooltip

#### 0.9.3 (2026-08-01)

- **宽度锁定后仍可拖拽调整**：锁定宽度后右缘缩放手柄保持可用，拖拽得到的新宽度自动继承为锁定宽度，无需先解锁再调整
- **默认不透明度调整 + 重置按钮**：字体透明度默认 25% → 80%、背景透明度默认 50% → 90%；设置界面为两项各新增「重置为初始值」按钮（lucide `rotate-ccw`），点击恢复默认值并实时应用到全部窗口
- **行首缩进归一化**：单行提词渲染前一律去掉行首 tab/空格，嵌套列表等缩进行不再被 Markdown 判为缩进代码块（`pre/code`），按无缩进列表/文本展示
- **宽度测量纳入渲染类上下文**：宽度自适应探针中的内容子元素包一层 `markdown-rendered`/`markdown-preview-view` 渲染类，列表符号等由这些类渲染的伪元素宽度被正确量到，修复列表项末尾字符换行

#### 0.9.2 (2026-08-01)

- **新增「隐藏背景」按钮**：位于宽度锁定按钮之后，激活后提词器背景全透明（含悬停/拖拽态）；未激活时背景色与透明度照常引用设置界面所设的背景透明度
- **背景常显**：提词器背景色与边框改为常显（透明度取设置），不再仅悬停时显示；悬停/拖拽仅附加阴影
- **穿透锁定不再强制全透明**：穿透锁定只负责鼠标事件穿透，背景全透明统一由「隐藏背景」按钮控制
- **工具栏布局优化**：穿透锁定按钮移至字体大小按钮之前；工具栏背景宽度自适应按钮区域并居中，不再撑满窗口
- **穿透锁定时精简工具栏**：仅保留上一项/下一项、穿透锁定、关闭等交互按钮，隐藏「锁定当前文档」与「模式切换」
- **面板视觉迁移**：边框/圆角/背景/阴影/过渡等面板视觉从窗口根元素移至内容区外层容器，与工具栏分离
- **自定义按钮提示**：替换 Obsidian 原生 tooltip，默认在上方弹出、上方无空间时自动翻转到底部；文案精简（去掉括号及内容）
- **文档绑定优化**：已锁定文档后切换文档再点击绑定按钮，现在直接解除锁定，不再转向锁定新文档
- **修复拖动提词器导致背后文档光标移动**：内容区挂 `markdown-preview-view` 类被 Obsidian 阅读视图 CSS 设为 `user-select: text`，拖窗时浏览器原生文本选择会延伸到背后编辑器；拖拽时 `preventDefault` 阻止原生选择，并显式将内容区设为 `user-select: none`

#### 0.9.1 (2026-08-01)

- **修复行模式不跟随光标**：workspace / editor 级 `cursor-move` 事件在部分环境不可靠，改为**轮询跟随**——每 150ms 读取跟随文档（绑定 > 活动）编辑器光标，行号或选中变化即重新提取；光标行未变时手动上一项/下一项浏览不受干扰
- **跟随编辑器缓存**：文档/叶子切换、绑定变化时重解析并缓存编辑器，轮询直接读缓存，避免每轮遍历叶子；编辑器销毁（叶子重建）自动重连
- **移除冗余事件链路**：删除 manager 级 `cursor-move` 转发与编辑器 `on('cursor-move')` 监听，单一轮询路径，无重复渲染
- **模式切换按钮改为文本**：显示当前模式「逐行提取」/「高亮提取」，点击切换，激活态高亮
- **工具栏图标语义化**：上一项/下一项改用 `arrow-big-left` / `arrow-big-right`；穿透锁定改用 `lock`/`unlock`（复用宽度锁定图标模式），宽度锁定图标改为 `move-horizontal`；字体大小按钮图标随档位匹配 `heading-1` ~ `heading-5`
- **设置按钮直达提词器设置页**：先打开设置弹窗，再跳转 Glimpse 插件页签并预置「提词器」分区

#### 0.9.0 (2026-07-31)

- **新增提词器（桌面端）**：桌面歌词式浮动提词窗口，支持多实例，见 [docs/adr/0001-teleprompter-floating-overlay.md](docs/adr/0001-teleprompter-floating-overlay.md)
- **三种内容模式**：行提取（跟随光标显示当前行）、高亮提取（顺序显示 `==...==` 高亮匹配）、选中提取（选中文本临时覆盖，取消选中自动恢复）
- **高亮索引联动**：点击索引卡片自动绑定该文档并切换到高亮提取模式定位该匹配
- **文档绑定**：固定提取来源文档，不再跟随活动文档
- **上一项/下一项**：工具栏按钮或滚轮切换（行模式严格 ±1，不循环）
- **穿透锁定**：整窗 `pointer-events` 穿透（仅保留交互按钮），悬浮提示内容不遮挡编辑
- **宽度自适应 + 锁定**：内容/工具栏自动适配宽度，右缘拖拽调整，可锁定
- **拖拽吸附**：视口边缘/中心线吸附，贴近中心线显示辅助线
- **字体大小循环**：32/40/50/64/80px 五档循环
- **透明度设置**：字体透明度（默认 25%，占位显示上一项文本时减半）与背景透明度（默认 50%）滑块，实时生效
- **状态持久化**：窗口位置/尺寸/锁定/模式/绑定在重启后自动恢复
- **空内容回退**：无文本时显示上一项内容（半透明占位）
- **命令**：新增「打开提词器」（复用最近活跃实例）与「关闭所有提词器」
- **渲染**：内容经 MarkdownRenderer 渲染，支持 Markdown 格式
- **状态栏入口**：右下角状态栏新增「打开提词器」按钮（lucide-presentation），设置中可开关（默认开），悬停样式与状态栏原生按钮一致
- **主题/自定义 CSS 复用**：提词器内容经 `markdown-rendered` 类复用主题与自定义 CSS 样式（标题、代码块、内联格式等），仅字体大小由提词器控制
- **宽度自适应修复**：改为测量内容真实最宽行（隐藏 nowrap 探针克隆），上一项/下一项切换不再逐次增宽；宽度钳制视口上限，超宽长文本换行不溢出屏幕
- **拖宽自动锁定**：手动拖动右缘调整宽度后自动锁定，防止内容变化重排；点击锁定按钮解锁恢复自适应
- **工具栏样式**：图标居中、背景复用状态栏背景色、按钮统一 `clickable-icon` 悬停样式、垂直居中
- **修复提词器无法打开**：三处运行时崩溃（`rootEl` 未定义引用、`ButtonComponent` 未导入、按钮字段误存 `HTMLButtonElement` 导致 `setTooltip` 不可用）
- **TypeScript 升级**：4.4.4 → 7.0.2，`npm run dev`/`build` 前置 `tsc --noEmit` 类型检查，构建期拦截类型错误
- **API 适配**：`MarkdownRenderer.renderMarkdown` 异步渲染（完成后按内容重新适配宽度）、CM `ViewUpdate.geometryChanged` 重命名（旧名在新版恒失效）、`cursor.value` 联合类型收窄
- **整窗拖拽**：拖拽区域覆盖整个提词器（按钮/缩放手柄除外），4px 移动阈值保护内容区链接点击，拖拽后抑制误触 click

#### 0.8.3 (2026-07-24)

- **修复捕获组对多行正则不生效**：`getDeco` 中 `cursor instanceof RegExpCursor` 检查在正则含 `\s`/`\n` 等时构造函数返回 `MultilineRegExpCursor`，该独立类不继承 `RegExpCursor`，捕获组逻辑始终跳过；改用 `match?.indices?.groups` 无类型守卫判断
- **新增无名捕获组支持**：捕获组装饰统一使用自定义样式类名，不再依赖 `(?<name>)` 命名。支持无名组 `(…)` 通过 `match.indices[1..N]` 自动匹配
- **修复开关关闭时捕获组/父行/开始/结束装饰仍生效**：line/start/end/group 四种标记类型各自独立检查 `mark.includes()`，开关仅增删 `"match"`；引入 `enabled` 标志统一控制所有装饰
- **修复匹配与捕获组共存**：勾选「捕获组」时自动跳过整段匹配装饰，二者互斥

#### 0.8.2 (2026-07-24)

- **修复保存自定义样式不生效**：`updateStaticHighlighter` 重构时移除了 `Compartment` 和 `iterateCM6` 调度，导致已打开编辑器中的 Facet 值未更新，ViewPlugin 不会重绘；恢复完整 compartment 架构
- **修复开关切换样式不立即生效**：`reconfigureStaticHighlighter` 仅传入 Facet 配置遗漏了 `staticHighlighter` ViewPlugin，compartment 替换时 ViewPlugin 丢失 → 需重启生效；现在 reconfigure 同时包含 ViewPlugin 和 Facet
- **完善自定义样式功能介绍**：补充标记类型、自定义 CSS 注入机制、分组管理、导入导出等详细说明

#### 0.8.1 (2026-07-24)

- **修复捕获组不生效**：`regexp-cursor.ts` 正则 flags 缺 `d`（`hasIndices`），改用原生 `this.re.exec()` 替代 `regexp-match-indices` polyfill
- **修复捕获组位置偏移错误**：`linePos + groupFrom` 改为 `(from - match.index) + groupFrom`，单行/多行光标通用
- **修复编译阻塞**：`esbuild.config.mjs` 移除 `watch` 选项，升级 esbuild 至 `0.25.12`

#### 0.8.0 (2026-07-23)

- **高亮索引代码拆分**：将 `HighlightIndexView` 从 `main.ts` 提取为独立模块 `src/highlight-index-view.ts`，消除主文件膨胀
- **标题层级分组**：高亮索引支持按文档标题结构分组展示，每个匹配项归入最近的前置标题下
- **祖先标题追溯**：展示每个标题的父标题链（祖先面包屑路径），首个索引项前集中展示共享祖先
- **标题等级图标与配色**：h1-h6 标题使用 `setIcon(heading-1~6)` lucide 图标，`--h1-color` ~ `--h6-color` CSS 变量着色；祖先标题复用同套样式
- **文档名标题**：索引顶部居中显示当前文档名
- **孤立匹配前置**：无标题归属的高亮项（文档标题之前的内容）置于最前，以文档名为分组名
- **标签页切换 Bug 修复**：点击索引条目不再因焦点切换清空内容回退到上一个文档的索引
- **README 补充**：新增「高亮索引」功能介绍

#### 0.7.2 (2026-07-19)

- **高亮索引**：新增「高亮索引」侧边栏标签页，自动检索文档中 `==...==` 包裹的 Obsidian 标准高亮文本
- **毛玻璃卡片**：高亮文本以圆角毛玻璃卡片展示，支持悬浮增强毛玻璃特效（0.08s 快速过渡）
- **一键复制**：每张卡片右上角添加复制按钮（lucide clipboard-paste），点击复制文本内容并弹出提示
- **自动检索**：切换文档时自动刷新高亮索引；新焦点文档无高亮时保留上次检索结果
- **失焦保留**：焦点离开 Markdown 编辑器时仍展示最后文档的高亮结果
- **命令面板**：注册「打开高亮索引」命令，可从命令面板呼出
- **可选中文本**：卡片内文字可选中，字体为正文字体 0.8 倍
- **设置开关**：「默认打开高亮索引」开关，开启后插件启动时自动启用高亮索引标签页
- **唯一实例**：保证高亮索引标签页只同时存在一个，重载时正确清理
- **设置 UI 重构**：持久高亮/选择高亮/高亮索引三标签页切换架构，代码拆分为独立模块

#### 0.7.1 (2026-07-19)

- **缩略图重构**：固定宽度 120px，字体大小和换行根据编辑器每行最大字符数自适应，逐字符按像素宽度换行兼容中英混排
- **HiDPI 适配**：`ctx.scale(dpr, dpr)` 统一坐标系为 CSS 像素，字号、换行、行高不受设备像素比影响
- **滚动偏移渲染**：缩略图内容跟随编辑器滚动偏移，滑块拖到底时显示文档最后一行；拖拽滑块也按视觉内容高度映射
- **即时渲染**：滚动事件触发全量重绘（含偏移），消除滚动延迟
- **性能优化**：逐字 `measureText` 改为字符宽度查表（半角/全角各测一次再整数累加），长文档卡顿大幅减少
- **默认关闭**：缩略图设置默认值改为 `false`，需手动在设置中开启
- **行高修正**：`fontSize × 1.3` 文本行、`fontSize × 0.65` 空行，避免重叠
- 翻译：`src/highlighters/minimap.ts` 全部使用中文注释

#### 0.7.0 (2026-07-18)

- **滚动条标记**：选中文本时在滚动条上显示匹配位置矩形，使用主题强调色 `--interactive-accent`，canvas 实现
- **缩略图（Minimap）**：编辑器右侧显示文档缩略图（类似 VS Code minimap），支持拖拽滑块滚动、点击跳转、滚轮同步，可独立开关
- **全文档搜索**：选中匹配不再限于可见区域，使用 `SearchCursor` 遍历全文
- **匹配行采集与装饰限制分离**：先采集所有匹配行号再限制装饰数量，滚动条标记覆盖全文不受 `maxMatches` 限制
- **反向选区修复**：从锚点拖选到光标左侧时（`from > to`），规范化边界确保 `sliceDoc` 不返回空串
- **防抖修复**：默认延迟 200→0 的问题，`highlightDelay` 的 Facet combine 策略 `Math.min` → `Math.max`
- **过期 setTimeout 防护**：`clearVersion` 计数器避免旧 `setTimeout` 擦除新装饰
- **设置持久化**：`minSelectionLength`、`maxMatches` 强制跟随 `DEFAULT_SETTINGS`，data.json 残留值自动写回
- **中英文注释**：`selection.ts`、`scrollbar-markers.ts` 添加完整中文注释

- **插件重命名**：从 GazerPlugin 统一改为 GlimpsePlugin，更新所有文件中的类型引用、CSS 类名和导出文件名
- **修复新建高亮器不生效的 Bug**：`static.ts` 中错误使用了非标准数组方法 `.contains()`（返回 `undefined`），导致新保存的高亮器 `mark` 数组虽包含 `"match"` 却无法激活替换为 `.includes()`
- **设置 UI 全面 CSS 重构**：遵循 Obsidian 插件审核规范，移除所有内联 `setAttribute("style")`，改用 CSS 自定义属性（`--picker-bg`）和样式类（`.glimpse-modal-input`、`.highlighter-name`）；新增 `styles.css` 规则
- **新增从剪贴板导入**：保存按钮前添加带 `clipboard-copy` 图标的导入按钮，解析剪贴板 JSON 填充表单全部字段（名称、颜色、查询、正则、标记开关、自定义 CSS），自动适配暗/亮主题编辑器
- **新增单个高亮器导出**：编辑和删除按钮之间添加带 `clipboard-paste` 图标的导出按钮，可单独导出某个自定义样式的 JSON
- **新建表单增加"匹配"开关**：新建高亮器时默认开启，直观显示该样式保存后将处于激活状态；父行/开始/结束/捕获组默认关闭
- **工具栏按钮更名**："导入" → "一键导入"，"导出" → "一键导出"
- **代码清理**：`regexp-cursor.ts` 统一空白和格式风格

#### 0.5.0 (2026-07-18)

- TypeScript: 所有类属性添加 definite assignment 断言，移除未使用的 `customCSS` 属性及接口
- 修复 `updateConfig` 中 `reconfigureSelectionHighlighter` 类型不匹配问题
- tsconfig: 移除已弃用的 `baseUrl`，启用 `strict`，`moduleResolution` 改为 `bundler`
- 新增 `.clickable-icon:hover` 反主题渗透样式，使用 `--icon-color-hover` 和 `--background-modifier-hover`
- 新建高亮器时父行/开始/结束开关默认关闭

#### 0.4.0 (2026-07-17)

- 新增标签组功能：支持创建、重命名、删除标签组，自定义样式可归类管理
- 支持拖拽自定义样式到标签组标签页上，带悬停动画效果
- 添加标签组按钮默认弹出命名对话框
- 导入兼容旧格式数据（无标签组 → 全部归入"默认"）
- 标签组及其操作按钮全面使用 Obsidian 原生 `.clickable-icon` 样式
- 标签组 UI 重新设计：0 间隙排列、强调色激活标签、圆形上角/方形下角、贯穿分隔线
- TypeScript 严格模式错误修复

#### 0.3.2 (2026-07-17)
- 拖拽图标改为 Obsidian 原生 Lucide `grip-vertical`
- 拖拽图标和颜色预览整合进 `highlighter-details` 内展示
- 移除 `highlighter-item-draggable` 冗余容器
- 设置项描述仅保留搜索表达式/搜索词，移除 CSS 类名和颜色
- 解锁中文选择高亮：`minSelectionLength` 默认值 3→1
- 修复 `.query-wrapper input` 宽度 `15ch` 导致切换按钮遮挡保存按钮
- TypeScript 严格模式全面清理，修复所有类型错误和模块导入问题
